import 'package:flutter/material.dart';

class GridPage {
  static Widget getPage(int index) {
    switch (index) {
      case 0:
        return const Placeholder();
      default:
        return const Placeholder();
    }
  }
}
